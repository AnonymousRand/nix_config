# `modules/utils/`

reusable util aspects. to use one in an aspect, simply include it and set any declared options if appropriate.

### notes

- often these options are declared inside a class module instead of on the aspect level itself, since accessing aspect-level options from within a class module is kinda hard in den...
