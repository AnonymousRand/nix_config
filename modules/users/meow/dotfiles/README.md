# Dotfiles

Put dotfiles (from `~/.config/`) that should be copied verbatim by Home Manager (using `builtins.readFile`, or `home.file` if that's not available like for vim colorscheme files) to `~/.config/` here. This is for configs that are rather annoying to state declaratively using Home Manager's native options.
