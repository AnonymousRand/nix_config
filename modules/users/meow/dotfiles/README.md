# Dotfiles

Put dotfiles (from `~/.config/`) that should be copied verbatim by Home Manager (using `home.file`) to `~/.config/` here. This is for configs that are rather annoying to state declaratively using Home Manager's native options.
